<div class="container">
  <div class="row">
<% @cats.each do |cat| %>
<div class="thumbnail">
<h2><%= cat.name %></h2>
<p><%= cat.description %></p>
//<strong><%= cat.price %></strong>
<h2>Adresse: </h2>
<%= cat.address %>
<%= cat.zip_code %>
<%= cat.city %>

<div class="list-inline">
<li>
<%if cat.availability == false %>
    <button type="button" class="btn btn-danger">Non disponible</button>
<% else %>
    <button type="button" class="btn btn-success">Disponible</button>
<% end %>
</li>

<li><%= link_to "Voir", cat_path(cat), class: "btn btn-warning" %></li>
</div>

</div>
<% end %>


</div>
</div>
